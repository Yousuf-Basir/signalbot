{
  "mcpServers": {
    "signalbot": {
      "cwd": ".",
      "command": "python",
      "args": [
        "./mcp/bootstrap.py"
      ],
      "note": "Local stdio MCP server for reading the current user's Signal Desktop message database. The bootstrap script creates a plugin-local Python virtual environment and installs requirements on first run."
    }
  }
}
